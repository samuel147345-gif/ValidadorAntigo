<Window x:Class="ValidadorJornada.Views.ExportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:ValidadorJornada.Converters"
        Title="Gerar Documento de Jornadas" 
        Height="720" Width="650"
        MinHeight="720" MinWidth="650"
        ResizeMode="CanResize" 
        WindowStartupLocation="CenterOwner"
        Background="#FAFAFA">
    
    <Window.Resources>
        <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>
    </Window.Resources>
    
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Grid.Row="0" 
                   Text="📄 Gerar Documento de Jornadas" 
                   FontSize="16" 
                   FontWeight="Bold" 
                   Foreground="#2C3E50"
                   Margin="0,0,0,15"/>

        <!-- Data única -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <CheckBox x:Name="chkDataUnica"
                      Content="Usar mesma data para todas as jornadas"
                      IsChecked="True"
                      Checked="ChkDataUnica_Changed"
                      Unchecked="ChkDataUnica_Changed"
                      FontSize="12"
                      Margin="0,0,0,10"/>
            
            <StackPanel x:Name="pnlDataUnica">
                <TextBlock Text="Data da Alteração (todas)" 
                           FontSize="12" 
                           FontWeight="SemiBold" 
                           Foreground="#2C3E50" 
                           Margin="0,0,0,6"/>
                <DatePicker SelectedDate="{Binding DataReferencia}"
                            Padding="10"
                            FontSize="13"
                            Background="White"
                            BorderBrush="#BDBDBD"
                            BorderThickness="2"/>
            </StackPanel>
        </StackPanel>

        <!-- Lista de Jornadas -->
        <Border Grid.Row="2" 
                BorderBrush="#E0E0E0" 
                BorderThickness="2" 
                CornerRadius="6" 
                Background="White"
                Margin="0,0,0,15">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="lstJornadas" Margin="10">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#E0E0E0" 
                                    BorderThickness="1" 
                                    CornerRadius="4"
                                    Padding="12"
                                    Margin="0,0,0,10"
                                    Background="#FAFAFA">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Jornada -->
                                    <TextBlock Grid.Row="0" 
                                               Text="{Binding Jornada}" 
                                               FontSize="11" 
                                               FontWeight="Bold"
                                               Foreground="#2196F3"
                                               Margin="0,0,0,10"/>

                                    <!-- Matrícula -->
                                    <StackPanel Grid.Row="1" Margin="0,0,0,6">
                                        <TextBlock Text="Matrícula" FontSize="10" Foreground="#616161" Margin="0,0,0,2"/>
                                        <TextBox Text="{Binding Matricula, UpdateSourceTrigger=PropertyChanged}"
                                                 Padding="6" FontSize="11"
                                                 Background="White" BorderBrush="#BDBDBD" BorderThickness="1"/>
                                    </StackPanel>

                                    <!-- Nome -->
                                    <StackPanel Grid.Row="2" Margin="0,0,0,6">
                                        <TextBlock Text="Nome Completo" FontSize="10" Foreground="#616161" Margin="0,0,0,2"/>
                                        <TextBox Text="{Binding Nome, UpdateSourceTrigger=PropertyChanged}"
                                                 Padding="6" FontSize="11"
                                                 Background="White" BorderBrush="#BDBDBD" BorderThickness="1"/>
                                    </StackPanel>

                                    <!-- Cargo -->
                                    <StackPanel Grid.Row="3" Margin="0,0,0,6">
                                        <TextBlock Text="Cargo" FontSize="10" Foreground="#616161" Margin="0,0,0,2"/>
                                        <TextBox Text="{Binding Cargo, UpdateSourceTrigger=PropertyChanged}"
                                                 Padding="6" FontSize="11"
                                                 Background="White" BorderBrush="#BDBDBD" BorderThickness="1"/>
                                    </StackPanel>

                                    <!-- Data da Alteração (individual) -->
                                    <StackPanel Grid.Row="4" x:Name="pnlDataIndividual">
                                        <TextBlock Text="Data da Alteração" FontSize="10" Foreground="#616161" Margin="0,0,0,2"/>
                                        <DatePicker SelectedDate="{Binding DataAlteracao, UpdateSourceTrigger=PropertyChanged}"
                                                    Padding="6" FontSize="11"
                                                    Background="White" BorderBrush="#BDBDBD" BorderThickness="1"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Info -->
        <Border Grid.Row="3" 
                Background="#E3F2FD" 
                BorderBrush="#2196F3" 
                BorderThickness="2" 
                CornerRadius="6" 
                Padding="12"
                Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="ℹ️ Instruções:" 
                           FontSize="11" 
                           FontWeight="Bold" 
                           Foreground="#1976D2"
                           Margin="0,0,0,5"/>
                <TextBlock x:Name="txtInstrucoes"
                           TextWrapping="Wrap" 
                           FontSize="10" 
                           Foreground="#424242"/>
            </StackPanel>
        </Border>

        <!-- Botões -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Cancelar" 
                    x:Name="btnCancelar"
                    Padding="20,10" 
                    Margin="0,0,10,0"
                    Click="BtnCancelar_Click"
                    Background="White" 
                    Foreground="#616161"
                    BorderBrush="#BDBDBD" 
                    BorderThickness="2"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="6"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                         VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#F5F5F5"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
            
            <Button Content="📄 Gerar PDF" 
                    x:Name="btnGerar"
                    Padding="20,10"
                    Click="BtnGerar_Click"
                    IsEnabled="{Binding IsProcessing, Converter={StaticResource InverseBoolConverter}}"
                    Background="#2196F3" 
                    Foreground="White"
                    BorderThickness="0"
                    FontWeight="SemiBold"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border"
                                            Background="{TemplateBinding Background}"
                                            CornerRadius="6"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                         VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#1976D2"/>
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter TargetName="border" Property="Background" Value="#BDBDBD"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
